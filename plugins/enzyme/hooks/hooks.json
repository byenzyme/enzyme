{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/bin/enzyme\" --version >/dev/null 2>&1 || echo 'enzyme: bundled binary unavailable — see https://github.com/jshph/enzyme/releases' >&2; \"${CLAUDE_PLUGIN_ROOT}/bin/enzyme\" refresh --quiet >/dev/null 2>&1 || true"
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/bin/enzyme\" refresh --quiet >/dev/null 2>&1 || true"
          }
        ]
      }
    ]
  }
}
